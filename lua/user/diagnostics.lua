-- Diagnostics configuration (for vim.diagnostics.config({...})) when diagnostics are on
return {
  virtual_text = false,
  underline = false,
  update_in_insert = false,
  -- float = {
  --   prefix = "■ ",
  --   source = "always",
  -- },
}
