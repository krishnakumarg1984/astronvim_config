-- Diagnostics configuration (vim.diagnostics.config({}))
return {
  virtual_text = false,
  underline = false,
  update_in_insert = false,
  severity_sort = true, -- <---
  -- float = {
  --   prefix = "■ ",
  --   source = "always",
  -- },
}
